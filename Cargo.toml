[package]
name = "lau-noether-agents"
version = "0.1.0"
edition = "2021"
license = "MIT"
repository = "https://github.com/SuperInstance/lau-noether-agents"
description = "Noether's theorem for agent systems — every symmetry yields a conserved quantity, linking conservation laws to fleet symmetries"

[dependencies]
nalgebra = { version = "0.33", features = ["serde-serialize"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
